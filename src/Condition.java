public class Condition {
    // عددی که باید بر ان بخش پذیر باشه توی هر سری
    int divisor;
    // رشته دلخواه مان
    String output;

    public Condition(int divisor, String output) {
        this.divisor = divisor;
        this.output = output;
    }
}
